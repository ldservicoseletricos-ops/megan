export default function AutonomyMeritCard({ ledger, rebalancePlan }) { const items = ledger?.items || []; return <section className="card"><div className="card-header"><h3>Internal Merit</h3><span className="hint">Desempenho consolidado</span></div><div className="stats"><div><span>Cérebros</span><strong>{items.length}</strong></div><div><span>Líder atual</span><strong>{rebalancePlan?.leader?.name || '--'}</strong></div></div><div className="list">{items.slice(0,4).map((item) => <div key={item.id} className="list-item"><strong>{item.name}</strong><span>taxa {Math.round((item.metrics?.successRate || 0) * 100)}% · impacto {item.metrics?.avgImpact}</span></div>)}</div></section>; }
