export default function AutonomyPriorityBidsCard({ bids, loading, onRecalculate }) { const items = bids?.items || []; return <section className="card"><div className="card-header"><h3>Priority Bids</h3><button disabled={loading} onClick={() => onRecalculate?.()}>Recalcular</button></div><div className="list">{items.map((item) => <div key={item.missionId} className="list-item"><strong>{item.title}</strong><span>bid {item.bidScore} · urgência {item.urgency} · retorno {item.returnScore}</span></div>)}{!items.length ? <div className="hint">Sem lances de prioridade.</div> : null}</div></section>; }
