export default function AutonomyBrainMarketCard({ market, reputation, loading, onOpen }) { const items = market?.items || reputation || []; return <section className="card"><div className="card-header"><h3>Brain Market</h3><button disabled={loading} onClick={() => onOpen?.()}>Abrir mercado</button></div><div className="stats"><div><span>Liquidez</span><strong>{market?.liquidity ?? '--'}</strong></div><div><span>Attention Units</span><strong>{market?.attentionUnits ?? '--'}</strong></div><div><span>Price Index</span><strong>{market?.priceIndex ?? '--'}</strong></div></div><div className="list">{items.map((item) => <div key={item.id} className="list-item"><strong>{item.name}</strong><span>rep {item.reputation} · custo {item.operationalCost} · disponibilidade {item.availability}</span></div>)}{!items.length ? <div className="hint">Sem reputações internas ainda.</div> : null}</div></section>; }
