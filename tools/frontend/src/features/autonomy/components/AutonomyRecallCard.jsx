export default function AutonomyRecallCard({ recall, loading, onSearch }) { return <section className="autonomy-card"><header className="autonomy-card-header"><div><span className="autonomy-eyebrow">Recall</span><h3>Experiências parecidas</h3></div><button disabled={loading} onClick={() => onSearch?.({ query: 'frontend autonomia visual erro' })}>Buscar</button></header><p>{recall?.recommendation || 'Use a memória episódica para reduzir repetição de erros.'}</p><div className="autonomy-list">{(recall?.matches || []).slice(0,4).map((item)=><article key={item.id} className="autonomy-list-item"><strong>{item.title}</strong><span>{item.lesson}</span><small>relevância {item.relevance}</small></article>)}</div></section>; }
