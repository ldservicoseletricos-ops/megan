export default function AutonomyAlliancesCard({ items = [], loading, onCreate }) { return <section className="card"><div className="card-header"><h3>Alliances</h3><button disabled={loading} onClick={() => onCreate?.()}>Criar</button></div><div className="list">{items.map((item) => <div key={item.id} className="list-item"><strong>{item.name}</strong><span>{(item.members || []).join(' • ')} · synergy {item.synergy}</span></div>)}{!items.length ? <div className="hint">Nenhuma aliança ativa.</div> : null}</div></section>; }