export default function AutonomyFusionCard({ items, loading, onFuse }) { const entries = items || []; return <section className="card"><div className="card-header"><h3>Fusion Opportunities</h3><span className="hint">Sinergia e redução de redundância</span></div><div className="list">{entries.map((item) => <div key={item.id} className="list-item"><strong>{item.suggestedName}</strong><span>{item.leftBrainId} + {item.rightBrainId} · sinergia {item.synergyScore}</span><button disabled={loading} onClick={() => onFuse?.({ leftBrainId: item.leftBrainId, rightBrainId: item.rightBrainId, name: item.suggestedName })}>Fundir</button></div>)}{!entries.length ? <div className="hint">Nenhuma fusão sugerida.</div> : null}</div></section>; }
