export default function AutonomyResourceEconomyCard({ resources, loading, onRebalance }) { const items = resources?.items || []; return <section className="card"><div className="card-header"><h3>Resource Economy</h3><button disabled={loading} onClick={() => onRebalance?.()}>Rebalancear</button></div><div className="stats"><div><span>Score</span><strong>{resources?.score ?? '--'}</strong></div><div><span>Ativos</span><strong>{resources?.activeBrains ?? '--'}</strong></div><div><span>Unidades</span><strong>{resources?.utilizedUnits ?? '--'}/{resources?.totalUnits ?? '--'}</strong></div></div><div className="list">{items.map((item) => <div key={item.id} className="list-item"><strong>{item.name}</strong><span>{item.role} · {item.allocationUnits}u · eficiência {item.efficiency}</span></div>)}{!items.length ? <div className="hint">Sem alocação registrada.</div> : null}</div></section>; }