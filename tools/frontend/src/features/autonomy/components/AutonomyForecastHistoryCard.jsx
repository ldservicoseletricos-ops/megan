export default function AutonomyForecastHistoryCard({ history }) { const items = history?.items || []; return <section className="autonomy-card"><header className="autonomy-card-header"><div><span className="autonomy-eyebrow">Histórico</span><h3>Forecast History</h3></div></header><div className="autonomy-list">{items.length ? items.slice(0,5).map((item)=><article key={item.id} className="autonomy-list-item"><strong>{item.bestPath}</strong><span>{item.recommendation}</span><small>{item.createdAt}</small></article>) : <p className="autonomy-empty">Nenhuma previsão registrada ainda.</p>}</div></section>; }
