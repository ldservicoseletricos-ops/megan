export default function AutonomyRetirementCard({ items, loading, onRetire }) { const entries = items || []; return <section className="card"><div className="card-header"><h3>Retirement Queue</h3><span className="hint">Cérebros com baixa eficiência relativa</span></div><div className="list">{entries.map((item) => <div key={item.id} className="list-item"><strong>{item.name}</strong><span>score {item.score} · {item.reason}</span><button disabled={loading} onClick={() => onRetire?.({ brainId: item.brainId, status: 'retired' })}>Aposentar</button></div>)}{!entries.length ? <div className="hint">Nenhum cérebro em fila de aposentadoria.</div> : null}</div></section>; }
