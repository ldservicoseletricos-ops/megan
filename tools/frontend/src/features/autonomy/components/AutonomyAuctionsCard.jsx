export default function AutonomyAuctionsCard({ auctions, loading, onRun }) { const items = auctions?.items || []; return <section className="card"><div className="card-header"><h3>Auctions Live</h3><button disabled={loading} onClick={() => onRun?.(items[0]?.missionId)}>Rodar leilão</button></div><div className="list">{items.map((item) => <div key={item.id} className="list-item"><strong>{item.title}</strong><span>{item.priority} · vencedor {item.winner?.brainName || '--'} · valor {item.winner?.value ?? '--'}</span></div>)}{!items.length ? <div className="hint">Nenhuma licitação aberta.</div> : null}</div></section>; }
