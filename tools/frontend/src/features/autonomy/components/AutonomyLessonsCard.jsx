export default function AutonomyLessonsCard({ lessons }) { const items = lessons?.lessons || []; return <section className="autonomy-card"><header className="autonomy-card-header"><div><span className="autonomy-eyebrow">Lições</span><h3>Lessons Ledger</h3></div></header><div className="autonomy-list">{items.slice(0,5).map((item)=><article key={item.id} className="autonomy-list-item"><strong>{item.title}</strong><span>{item.lesson}</span><small>{item.impact}</small></article>)}</div></section>; }
