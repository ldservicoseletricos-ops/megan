export default function AutonomyCooperationLedgerCard({ items = [] }) { return <section className="card"><div className="card-header"><h3>Cooperation Ledger</h3></div><div className="list">{items.map((item) => <div key={item.id} className="list-item"><strong>{item.title}</strong><span>{item.type} · {item.status} · synergy {item.synergy}</span></div>)}{!items.length ? <div className="hint">Sem histórico de cooperação.</div> : null}</div></section>; }