export default function AutonomyProbabilityCard({ probability }) { const items = probability?.items || []; return <section className="autonomy-card"><header className="autonomy-card-header"><div><span className="autonomy-eyebrow">Probabilidade</span><h3>Probability Matrix</h3></div></header><div className="autonomy-meta-block"><span>Sucesso médio: {probability?.summary?.averageSuccess ?? 0}%</span><span>Regressão média: {probability?.summary?.averageRegression ?? 0}%</span></div><div className="autonomy-list">{items.slice(0,4).map((item)=><article key={item.scenarioId} className="autonomy-list-item"><strong>{item.title}</strong><span>Sucesso {item.success}% · Falha {item.failure}%</span><small>Valor esperado {item.expectedValue}</small></article>)}</div></section>; }
