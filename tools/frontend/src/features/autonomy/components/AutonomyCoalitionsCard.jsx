export default function AutonomyCoalitionsCard({ items = [], loading, onForm }) { return <section className="card"><div className="card-header"><h3>Coalitions</h3><button disabled={loading} onClick={() => onForm?.()}>Formar</button></div><div className="list">{items.map((item) => <div key={item.id} className="list-item"><strong>{item.missionTitle}</strong><span>{item.leader} + {(item.support || []).join(', ')} · synergy {item.synergy}</span></div>)}{!items.length ? <div className="hint">Nenhuma coalizão ativa.</div> : null}</div></section>; }