import { company } from '../config/company.js'; import state from '../data/state.json' with { type: 'json' }; export function getMarketingOverview(req, res) { res.json({ team: company.marketingTeam, metrics: state.marketing, recommendations: ['Definir posicionamento oficial da Megan','Criar landing page única','Publicar calendário semanal de conteúdo','Ativar fluxo de leads no WhatsApp e CRM','Criar anúncios com meta de validação inicial'] }); }
