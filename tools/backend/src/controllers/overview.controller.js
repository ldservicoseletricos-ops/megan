import { company } from '../config/company.js'; import state from '../data/state.json' with { type: 'json' }; import { getSupabaseStatus } from '../services/supabase.service.js'; import { getMemory } from '../services/memory.service.js'; export async function getOverview(req, res) { const supabase = await getSupabaseStatus(); const memoryData = await getMemory(); res.json({ company, state, memory: memoryData.memory, memorySource: memoryData.source, integrations: { supabase } }); }
