import { getMemory, updateMemory } from '../services/memory.service.js'; export async function getMemoryController(req, res) { const result = await getMemory(); res.json({ ok: true, source: result.source, memory: result.memory }); } export async function updateMemoryController(req, res) { const result = await updateMemory(req.body || {}); res.json({ ok: true, source: result.source, memory: result.memory }); }
