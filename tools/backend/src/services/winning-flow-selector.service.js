// Stub seguro gerado para estabilizar imports do ai-core.
// Mantém o módulo carregável sem remover funcionalidades existentes.

export function getWinningFlowSelectorOverview(input = {}) { return { ok: true, source: 'getWinningFlowSelectorOverview', input, items: [], status: 'stable', generatedAt: new Date().toISOString() }; }

export function evaluateWinningFlowSelector(input = {}) { return { ok: true, action: 'evaluateWinningFlowSelector', input, status: 'executed_safe_stub', generatedAt: new Date().toISOString() }; }
