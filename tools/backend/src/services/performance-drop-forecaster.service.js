// Stub seguro gerado para estabilizar imports do ai-core.
// Mantém o módulo carregável sem remover funcionalidades existentes.

export function getPerformanceDropOverview(input = {}) { return { ok: true, source: 'getPerformanceDropOverview', input, items: [], status: 'stable', generatedAt: new Date().toISOString() }; }

export function evaluatePerformanceDrop(input = {}) { return { ok: true, action: 'evaluatePerformanceDrop', input, status: 'executed_safe_stub', generatedAt: new Date().toISOString() }; }
