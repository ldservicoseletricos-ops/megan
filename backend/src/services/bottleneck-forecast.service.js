// Stub seguro gerado para estabilizar imports do ai-core.
// Mantém o módulo carregável sem remover funcionalidades existentes.

export function getBottleneckForecastOverview(input = {}) { return { ok: true, source: 'getBottleneckForecastOverview', input, items: [], status: 'stable', generatedAt: new Date().toISOString() }; }

export function evaluateBottleneckForecast(input = {}) { return { ok: true, action: 'evaluateBottleneckForecast', input, status: 'executed_safe_stub', generatedAt: new Date().toISOString() }; }
