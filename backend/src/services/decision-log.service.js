// Stub seguro gerado para estabilizar imports do ai-core.
// Mantém o módulo carregável sem remover funcionalidades existentes.

export function addDecisionLog(input = {}) { return { ok: true, action: 'addDecisionLog', input, status: 'executed_safe_stub', generatedAt: new Date().toISOString() }; }

export function getDecisionLogHistory(input = {}) { return { ok: true, source: 'getDecisionLogHistory', input, items: [], status: 'stable', generatedAt: new Date().toISOString() }; }
