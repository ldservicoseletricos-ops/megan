// Stub seguro gerado para estabilizar imports do ai-core.
// Mantém o módulo carregável sem remover funcionalidades existentes.

export function getModuleAutorebuildOverview(input = {}) { return { ok: true, source: 'getModuleAutorebuildOverview', input, items: [], status: 'stable', generatedAt: new Date().toISOString() }; }

export function evaluateModuleAutorebuild(input = {}) { return { ok: true, action: 'evaluateModuleAutorebuild', input, status: 'executed_safe_stub', generatedAt: new Date().toISOString() }; }
