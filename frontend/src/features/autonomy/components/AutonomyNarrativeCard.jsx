export default function AutonomyNarrativeCard({ narrative }) { const phases = narrative?.phases || []; return <section className="autonomy-card"><header className="autonomy-card-header"><div><span className="autonomy-eyebrow">Narrativa</span><h3>{narrative?.currentChapter || 'Narrativa interna'}</h3></div></header><p>{narrative?.summary || 'Aguardando narrativa de aprendizado.'}</p><div className="autonomy-list">{phases.map((phase)=><article key={phase.phase} className="autonomy-list-item"><strong>{phase.phase}</strong><span>{phase.summary}</span><small>{phase.status}</small></article>)}</div></section>; }
