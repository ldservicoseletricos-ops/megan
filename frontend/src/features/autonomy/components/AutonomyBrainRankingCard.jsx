export default function AutonomyBrainRankingCard({ items, loading, onRebalance }) { const ranking = items || []; return <section className="card"><div className="card-header"><h3>Brain Ranking</h3><button disabled={loading} onClick={() => onRebalance?.()}>Rebalancear</button></div><div className="list">{ranking.map((item, index) => <div key={item.id} className="list-item"><strong>#{index + 1} {item.name}</strong><span>{item.role} · mérito {item.meritScore} · impacto {item.avgImpact}</span></div>)}{!ranking.length ? <div className="hint">Nenhum ranking disponível.</div> : null}</div></section>; }
