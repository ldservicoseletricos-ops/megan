export default function AutonomyBestPathCard({ bestPath }) { const path = bestPath?.bestPath; return <section className="autonomy-card"><header className="autonomy-card-header"><div><span className="autonomy-eyebrow">Melhor caminho</span><h3>Decision Forecast</h3></div></header>{path ? <div className="autonomy-list"><article className="autonomy-list-item"><strong>{path.title}</strong><p>{bestPath.recommendation}</p><small>Score {path.score} · impacto {path.impact} · risco {path.risk}</small></article>{(path.actions || []).slice(0,3).map((action)=><article key={action} className="autonomy-list-item"><span>{action}</span></article>)}</div> : <p className="autonomy-empty">Nenhum caminho previsto.</p>}</section>; }
