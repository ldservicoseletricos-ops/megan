export default function AutonomyOpportunityAllocationCard({ allocation }) { const items = allocation?.items || []; const latest = allocation?.latest || items[0]; return <section className="card"><div className="card-header"><h3>Opportunity Allocation</h3></div><div className="stats"><div><span>Última missão</span><strong>{latest?.missionTitle || '--'}</strong></div><div><span>Vencedor</span><strong>{latest?.winner?.brainName || '--'}</strong></div><div><span>Suporte</span><strong>{latest?.supports?.length || 0}</strong></div></div><div className="list">{items.slice(0,4).map((item) => <div key={item.id} className="list-item"><strong>{item.missionTitle}</strong><span>{item.winner?.brainName || '--'} · {item.status}</span></div>)}{!items.length ? <div className="hint">Sem alocação registrada.</div> : null}</div></section>; }
