export default function AutonomyForecastScenariosCard({ scenarios, loading, onRun }) { const items = scenarios?.ranking || scenarios?.scenarios || []; return <section className="autonomy-card"><header className="autonomy-card-header"><div><span className="autonomy-eyebrow">Cenários 3.2</span><h3>Future Scenarios</h3></div><button disabled={loading} onClick={onRun}>Simular futuro</button></header><div className="autonomy-list">{items.slice(0,4).map((item)=><article key={item.id} className="autonomy-list-item"><div><strong>{item.title}</strong><p>{item.expectedOutcome || item.strategy}</p><small>{item.horizon} · risco {item.risk}</small></div><span className="autonomy-badge autonomy-status-active">{item.score ?? item.impact}</span></article>)}</div></section>; }
