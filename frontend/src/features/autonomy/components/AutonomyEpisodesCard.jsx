export default function AutonomyEpisodesCard({ episodes = [], loading, onCreate }) { return <section className="autonomy-card"><header className="autonomy-card-header"><div><span className="autonomy-eyebrow">Memória</span><h3>Episódios</h3></div><button disabled={loading} onClick={() => onCreate?.({ title: 'Novo episódio operacional', decision: 'record_current_learning', result: 'registered', impact: 'medium' })}>Registrar</button></header><div className="autonomy-list">{(episodes || []).slice(0,5).map((item)=><article key={item.id} className="autonomy-list-item"><strong>{item.title}</strong><span>{item.lesson}</span><small>{item.impact} • {item.decision}</small></article>)}</div></section>; }
